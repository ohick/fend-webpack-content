const path = require('path');
const webpack = require('webpack');

console.log("WAT");

module.exports = {
    mode: 'development',
    //entry: path.resolve(__dirname, 'src/client'),
    entry: './src/client'
};